server:
  port: 8081
spring:
  r2dbc:
    url: ${MYSQL_URL}
    username: ${MYSQL_USER}
    password: ${MYSQL_PASS}
  sql:
    init:
      mode: always
management:
  endpoints:
    web:
      exposure:
        include: health,info
