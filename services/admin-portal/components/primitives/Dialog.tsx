'use client';

// Re-export the Dialog implementation from `src/components` so imports using
// `@/components/primitives/Dialog` resolve correctly.
export { default } from '../../src/components/primitives/Dialog';
